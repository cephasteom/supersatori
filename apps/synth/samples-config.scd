// samples-config.scd
// List root directories to scan for sample banks.
// Each subdirectory becomes a named bank — e.g. ~/samples/kicks/ -> bank 'kicks'.
// Supports ~ expansion. Add as many directories as you like.

~sampleDirs = [
    // "~/samples",
    // "/path/to/more/samples"
];
